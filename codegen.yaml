schema: 'https://backboard.railway.com/graphql/v2'
documents: './src/wrappers/**/*.ts' # Path to GraphQL operation files
overwrite: true
generates:
  src/generated/graphql.d.ts:
    plugins:
      - 'typescript'
      - 'typescript-operations'